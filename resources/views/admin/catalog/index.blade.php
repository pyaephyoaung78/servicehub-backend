@extends('admin.layouts.app')
@section('title', 'Service Catalogue') @section('page_title', 'Service Catalogue')
@section('content')
<section class="mb-8 overflow-hidden rounded-xl bg-teal-950 px-5 py-6 shadow-[0_14px_32px_rgba(15,60,58,0.14)] sm:px-7 sm:py-7"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-sm font-medium text-teal-300">Service operations</p><h1 class="mt-2 text-2xl font-semibold tracking-tight text-white sm:text-3xl">Service catalogue</h1><p class="mt-2 max-w-2xl text-sm leading-6 text-teal-50/85">Control the services customers can book, their current pricing, duration, and category.</p></div><a href="{{ route('admin.catalog.services.create') }}" class="w-fit rounded-lg bg-white px-4 py-2.5 text-sm font-semibold text-teal-950 shadow-sm">Add service</a></div></section>
@if(session('success'))<div class="mb-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-800">{{ session('success') }}</div>@endif
<div class="grid gap-6 xl:grid-cols-5"><section class="rounded-xl border border-teal-100 bg-teal-50/40 p-5 xl:col-span-2"><h2 class="text-lg font-semibold text-teal-950">Categories</h2><form method="POST" action="{{ route('admin.catalog.categories.store') }}" class="mt-4 grid gap-3">@csrf<input name="name" required placeholder="New category name" class="h-11 rounded-lg border-teal-200 bg-white px-4 text-sm"><textarea name="description" rows="2" placeholder="Description (optional)" class="rounded-lg border-teal-200 bg-white px-4 py-3 text-sm"></textarea><input type="hidden" name="is_active" value="1"><button class="rounded-lg bg-teal-700 px-4 py-2.5 text-sm font-semibold text-white">Add category</button></form><div class="mt-5 space-y-3">@foreach($categories as $category)<form method="POST" action="{{ route('admin.catalog.categories.update', $category) }}" class="rounded-lg border border-teal-100 bg-white p-3">@csrf @method('PUT')<div class="flex gap-2"><input name="name" value="{{ $category->name }}" required class="h-10 min-w-0 flex-1 rounded-lg border-slate-200 px-3 text-sm"><input type="hidden" name="description" value="{{ $category->description }}"><input type="hidden" name="is_active" value="{{ $category->is_active ? 0 : 1 }}"><button class="rounded-lg px-3 text-xs font-semibold {{ $category->is_active ? 'bg-amber-50 text-amber-800' : 'bg-emerald-50 text-emerald-800' }}">{{ $category->is_active ? 'Deactivate' : 'Activate' }}</button></div><p class="mt-2 text-xs text-slate-500">{{ $category->services_count }} service{{ $category->services_count === 1 ? '' : 's' }}</p></form>@endforeach</div></section>
<section class="xl:col-span-3"><form method="GET" class="mb-6 rounded-xl border border-teal-100 bg-teal-50/40 p-5"><div class="grid gap-4 md:grid-cols-3"><input name="search" value="{{ request('search') }}" placeholder="Search services" class="h-11 rounded-lg border-teal-200 bg-white px-4 text-sm"><select name="service_category_id" class="h-11 rounded-lg border-teal-200 bg-white px-4 text-sm"><option value="">All categories</option>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(request('service_category_id') == $category->id)>{{ $category->name }}</option>@endforeach</select><select name="is_active" class="h-11 rounded-lg border-teal-200 bg-white px-4 text-sm"><option value="">All services</option><option value="1" @selected(request('is_active')==='1')>Active</option><option value="0" @selected(request('is_active')==='0')>Inactive</option></select></div><button class="mt-4 rounded-lg bg-teal-700 px-4 py-2.5 text-sm font-semibold text-white">Apply filters</button></form><div class="overflow-hidden rounded-xl border border-slate-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[720px] text-sm"><thead class="bg-teal-950 text-left text-xs font-semibold text-teal-50"><tr><th class="px-5 py-3">Service</th><th class="px-5 py-3">Price & duration</th><th class="px-5 py-3">Bookings</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-center">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($services as $service)<tr><td class="px-5 py-4"><p class="font-semibold text-slate-900">{{ $service->name }}</p><p class="text-xs text-slate-500">{{ $service->category?->name }}</p></td><td class="px-5 py-4 text-slate-600">{{ number_format((float)$service->base_price, 0) }} MMK<br><span class="text-xs">{{ $service->estimated_duration_minutes ? $service->estimated_duration_minutes.' min' : 'Duration not set' }}</span></td><td class="px-5 py-4 text-slate-600">{{ $service->bookings_count }}</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-medium {{ $service->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-600' }}">{{ $service->is_active ? 'ACTIVE' : 'INACTIVE' }}</span></td><td class="px-5 py-4 text-center"><a href="{{ route('admin.catalog.services.edit', $service) }}" class="mr-2 text-xs font-semibold text-teal-700">Edit</a><form method="POST" action="{{ route('admin.catalog.services.toggle', $service) }}" class="inline">@csrf @method('PATCH')<button class="text-xs font-semibold text-slate-600">{{ $service->is_active ? 'Deactivate' : 'Activate' }}</button></form></td></tr>@empty<tr><td colspan="5" class="px-5 py-12 text-center text-slate-500">No services match these filters.</td></tr>@endforelse</tbody></table></div>@if($services->hasPages())<div class="border-t border-slate-200 px-5 py-4">{{ $services->links() }}</div>@endif</div></section></div>
@endsection
